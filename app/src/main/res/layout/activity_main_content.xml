<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fadingEdge="vertical"
    android:fadingEdgeLength="30dp"
    android:requiresFadingEdge="vertical"
    tools:context=".mainPack.ui.MainContentActivity">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/main_content_view_pager2"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <RadioGroup
        android:id="@+id/main_radio_group"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="30dp"
        android:background="@color/main_18CAE4"
        android:elevation="15dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent">

        <RadioButton
            android:id="@+id/radio_button_home"
            style="@style/CustomRadioBtn"
            android:drawableTop="@drawable/main_home_selector"
            android:text="@string/title_home" />

        <RadioButton
            android:id="@+id/radio_button_donate"
            style="@style/CustomRadioBtn"
            android:drawableTop="@drawable/main_donate_selector"
            android:text="@string/title_donate" />

        <RadioButton
            android:id="@+id/radio_button_me"
            style="@style/CustomRadioBtn"
            android:drawableTop="@drawable/main_me_selector"
            android:text="@string/title_me" />
    </RadioGroup>

</androidx.constraintlayout.widget.ConstraintLayout>